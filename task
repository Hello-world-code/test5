工作场景如下：
	为实现某个新的需求，创建一个分支(dev).
	在这个分支上开展工作。
	正在此时，你突然接到一个电话说有个很严重的问题需要紧急修补。你将按照如下方式来处理：
	切换到你线上的分支(master/main)
	为这个紧急任务新建一个分支(fix),并在其中修复它
	在测试通过之后，切换回线上分支(master/main),然后合并这个修补分支(fix),最后将改动推送到线上分支(master).
	切换回你最初的工作分支上(dev),继续工作
